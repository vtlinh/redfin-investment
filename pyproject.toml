[project]
name = "mai-investment"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
